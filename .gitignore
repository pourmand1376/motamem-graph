# dependencies
node_modules/

# archived-page cache — large + regenerable via `just fetch`
cache/

# intermediate + generated data — regenerable via the pipeline
data/

# build outputs — regenerated by `just build`
public/app.js
public/graph.json

# OS / editor / logs
.DS_Store
*.log
